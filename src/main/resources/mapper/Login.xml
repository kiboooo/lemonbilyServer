<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lemonbily.springboot.mapper.LoginMapper">
    <select id="findAll" resultType="Login">
        select * from login
    </select>

    <!--<insert id="add" parameterType="com.lemonbily.springboot.entity.Login" >-->
         <!--insert into Login ( Name,LPassWord,LPhone ) values (#{Name},#{LPassWord},#{LPhone})-->
    <!--</insert>-->


</mapper>



